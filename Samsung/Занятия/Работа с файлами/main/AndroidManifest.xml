<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.kutergin17122019">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!--запрашиваем права на доступ к чтению и записи-->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <!--запрос прав на доступ в интернет-->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".samsung.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".samsung.Game"/>
        <activity android:name=".samsung.FileActivity"/>
        <activity android:name=".samsung.FileActivityStrorage"/>
    </application>

</manifest>